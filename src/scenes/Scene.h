#ifndef SCENE_H
#define SCENE_H
//场景基类
class Scene
{
public:
    Scene();
    // virtual ~Scene();
    // virtual void Init() = 0;
    // virtual void Update(float dt) = 0;
    // virtual void Render() = 0;
    // virtual void HandleInput() = 0;
};
#endif // SCENE_H